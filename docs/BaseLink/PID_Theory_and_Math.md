# PID Theory and Mathematical Modelling

This document strictly defines the mathematical principles governing the stabilization of the inverted pendulum configuration, exploring precisely why distinct elements were constructed algorithmically.

## 1. The Core Physics of an Inverted Pendulum
The Self-Balancing Robot is fundamentally an unstable mass interacting constantly with gravity. 

To maintain equilibrium, a compensating lateral force $F$ must be applied directly beneath the center of mass counter-acting the rotational torque $\tau$ generated by the angular deviation from absolute vertical $\theta$.
Because the physical structure mounts Stepper Motors mapping identical steps to linear tire displacement, applying acceleration equates directly to applying correcting force.

## 2. Discrete PID Implementation
The core mechanism calculating acceleration based on offset is the Proportional-Integral-Derivative controller, acting simultaneously on the deviation from the required pitch angle.

$$ Output = P \times \theta_{err} + I \times \int \theta_{err} \> dt + D \times \left(-\frac{d \theta_{measured}}{dt}\right) $$

### 2.a Validating Derivative on Measurement
Standard implementations compute the derivative exclusively across the unified Error state: $D_{error} = Error_{current} - Error_{previous}$.

This methodology fundamentally fails in joystick-enabled balancing applications. If the User radically alters the Setpoint Target Angle from $0.0^{\circ}$ to $3.0^{\circ}$ over a single control frame, the instantaneous math registers a massive synthetic jump in Error that physically does not exist causing catastrophic robotic thrashing and failure.
By measuring the physical differential relative solely to the IMU measurements ($d \theta_{measured} / dt$), instantaneous setpoint jumps exert absolutely zero influence on the reactive derivative constraints.

### 2.b Cascaded Derivative EMA Filtering
Derivatives react viciously to high-frequency IMU electromagnetic quantization spikes. To preserve the Stepper coil integrity, a 1st-Order Infinite Impulse Response (IIR) filter is applied mathematically solely upon the D term before summation:

$$ D_{final} = (\alpha \times D_{raw}) + ((1 - \alpha) \times D_{previous}) $$

Configured safely at $\alpha = 0.7$, this preserves rapid impulse reactions required for stability while brutally stripping microscopic 500Hz resonance frequencies propagated naturally by vibrating motor stators.

### 2.c Adaptive Reactive Gains 
When the robotic structure impacts anomalous topographical variants (large cables, stones, hard flooring transitions) the $\theta$ angular deviation exceeds standard norms dramatically.
If error functionally exceeds the defined `ADAPTIVE_ERROR_THRESHOLD` ($2.0^{\circ}$), a binary algorithmic logic aggressively multiplies the standard $K_p$ constraint structurally.
$$ K_{p\_effective} = K_p \times 1.5 $$
This artificially maximizes current constraints aggressively forcing the motors to "shove" harder when close to failure.

## 3. Position Maintenance Topology (Odometry Hold)
A robot exhibiting pure generic leveling behavior wanders chaotically due to minor structural chassis imbalances and unmeasurable frictional biases in distinct motor shafts.

To establish absolute spatial persistence, the continuous integral accumulation generated natively by the precise Bresenham Stepper Timers defines the robot's strict odometric origin relative to the universe.

$$ Pos_{error} = Pos_{current} - Pos_{held} $$
$$ Angle_{Correction} = \text{Constrain}(Pos_{error} \times K_{P\_Pos}) $$

If the robot deviates physically from the held coordinate without receiving specific traversal commands, the discrepancy generates an algorithmic angle tilt artificially modifying the primary Setpoint explicitly, mathematically coercing the machine to actively reverse course and zero the mechanical discrepancy precisely. 

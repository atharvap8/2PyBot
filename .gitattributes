# Normalize line endings for CI compatibility
# Python and YAML files must use LF (ruff and GitHub Actions require it on Linux runners)
*.py    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf

# Keep all other text files using the platform default
*       text=auto
